class Stats {
    title: string;
    views: number;
    image: string;

    constructor(title: string, views: number, image: string) {
        this.title = title;
        this.views = views;
        this.image = image;
    }
}

export default Stats;
